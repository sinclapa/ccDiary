{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "System": "Warning"
      }
    }
  },
  "Entra": {
    "Instance": "https://login.microsoftonline.com/",
    "ClientId": "OVERRIDE_VIA_ENVIRONMENT_VARIABLES",
    "TenantId": "OVERRIDE_VIA_ENVIRONMENT_VARIABLES",
    "ApplicationIdUri": "OVERRIDE_VIA_ENVIRONMENT_VARIABLES"
  },
  "Graph": {
    "TenantId": "OVERRIDE_VIA_ENVIRONMENT_VARIABLES",
    "ClientId": "OVERRIDE_VIA_ENVIRONMENT_VARIABLES",
    "ClientSecret": "OVERRIDE_VIA_ENVIRONMENT_VARIABLES",
    "InviteRedirectUrl": "OVERRIDE_VIA_ENVIRONMENT_VARIABLES",
    "AppDisplayName": "Cooking Code Diary"
  },
  "Smtp": {
    "Host": "OVERRIDE_VIA_ENVIRONMENT_VARIABLES",
    "Port": 587,
    "Username": "OVERRIDE_VIA_ENVIRONMENT_VARIABLES",
    "Password": "OVERRIDE_VIA_ENVIRONMENT_VARIABLES",
    "From": "OVERRIDE_VIA_ENVIRONMENT_VARIABLES",
    "FromName": "ccDiary"
  },
  "BootstrapAdmin": {
    "ObjectId": "",
    "Email": "",
    "DisplayName": ""
  },
  "AllowedHosts": "*",
  "Management": {
    "Endpoints": {
      "Actuator": {
        "Exposure": {
          // Named rather than "*": ingress is external and unauthenticated, so the exposed
          // set should be the two actuators the app actually registers, not whatever a
          // future package reference happens to add.
          "Include": [ "health", "info" ]
        }
      },
      "Health": {
        // Kept on: the deploy workflow gates a release on details.db.status.
        "ShowDetails": "Always"
      }
    }
  }
}
